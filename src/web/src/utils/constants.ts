/**
 * UI Constants
 * Shared constants for consistent UI behavior across the application
 */

/**
 * Default duration (in milliseconds) to display success/info messages before auto-dismissing
 */
export const SUCCESS_MESSAGE_TIMEOUT_MS = 3000;

/**
 * Modal backdrop class for consistent styling
 * Uses Tailwind classes for a semi-transparent black overlay
 */
export const MODAL_BACKDROP_CLASS = 'fixed inset-0 bg-black/90 flex items-center justify-center z-50 p-4';
