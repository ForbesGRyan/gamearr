# ===========================================
# Gamearr Environment Configuration
# ===========================================
# Copy this file to .env and customize as needed.
# Most settings are configured via the web UI (Settings page),
# but these environment variables control the server itself.

# ===========================================
# Server Configuration
# ===========================================

# Port the server listens on
PORT=7878

# Data path - where the SQLite database and logs are stored
# Docker users: mount a volume to /config and set DATA_PATH=/config
DATA_PATH=./data

# Disable ONNX-based semantic search (useful if ONNX fails to load)
# DISABLE_SEMANTIC_SEARCH=true

# ===========================================
# OPTIONAL: Pre-seed settings on first run
# ===========================================
# These are only used if the setting doesn't already exist in the database.
# After first run, use the Settings page in the web UI to change them.

# IGDB API Credentials (required for game metadata)
# Get these from: https://dev.twitch.tv/console
# IGDB_CLIENT_ID=your_client_id
# IGDB_CLIENT_SECRET=your_client_secret

# Prowlarr (required for indexer searches)
# PROWLARR_URL=http://localhost:9696
# PROWLARR_API_KEY=your_api_key

# qBittorrent (required for downloads)
# QBITTORRENT_HOST=http://localhost:8080
# QBITTORRENT_USERNAME=admin
# QBITTORRENT_PASSWORD=adminadmin

# Library path (where games are organized)
# LIBRARY_PATH=/path/to/games

# ===========================================
# OPTIONAL: Library Integrations
# ===========================================
# These integrate with external game libraries for import/sync.

# Steam Integration
# Get API key from: https://steamcommunity.com/dev/apikey
# Find your Steam ID at: https://steamid.io
# STEAM_API_KEY=your_steam_api_key
# STEAM_ID=your_steam_id

# GOG Integration
# Obtain refresh token via GOG Galaxy authentication flow
# GOG_REFRESH_TOKEN=your_gog_refresh_token
